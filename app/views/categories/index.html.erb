<h1>Categorías</h1>

<div class="row">
  <% @categories.each do |category| %>
    <div class="col-md-4 mb-4">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title">
            <%= link_to category.name, category_path(category), class: "text-decoration-none text-dark" %>
          </h5>
        </div>
      </div>
    </div>
  <% end %>
</div>
