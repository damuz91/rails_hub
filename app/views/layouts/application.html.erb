<!DOCTYPE html>
<html lang="en">
  <head>
    <title>RailsHub</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <%= yield :head_scripts %>


    <style>
      body {
        display: flex;
        flex-direction: column;
        min-height: 100vh;
        background-color: #181818;
        color: #fff;
        margin: 0;
      }
      .btn-highlight {
        background-color: #f90;
        color: #000;
        font-weight: bold;
      }
      .btn-highlight:hover {
        background-color: #ffcc00;
      }
      .video-thumbnail {
        border: 2px solid #333;
        border-radius: 5px;
      }
      .content {
        flex: 1; /* Hace que esta sección ocupe todo el espacio disponible */
      }
      footer {
        background-color: #000;
        color: #aaa; /* Contraste para que el texto sea legible */
      }
      footer a {
        color: #ffb400;
        text-decoration: none;
      }
      footer a:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>

    <%= render 'layouts/header' %>
    

    <!-- Contenido principal -->
    <div class="container content mt-4">
      <%= yield %>
    </div>

    <!-- Pie de página -->
    <footer class="py-4 text-center">
      <div class="container">
        <p class="mb-1">© 2025 RailsHub - Todos los derechos reservados</p>
        <p><a href="/terms">Términos y Condiciones</a> | <a href="/privacy">Política de Privacidad</a></p>
      </div>
    </footer>
  </body>
</html>
